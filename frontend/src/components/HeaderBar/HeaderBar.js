import { Header } from "../Header";
import { Dropdown } from "../Dropdown";

const HeaderBar = () => {
    return (
        <div id="header-bar">
            <Header />
            <Dropdown />
        </div>
    )
}

export default HeaderBar